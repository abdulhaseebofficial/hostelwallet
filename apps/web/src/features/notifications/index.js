/**
 * What notifications offers the rest of the app.
 *
 * The bell and its tray.
 *
 * Anything not re-exported here is internal to this feature. Other features
 * and the app shell import from this file, never from a path inside it.
 */

export { default as NotificationBell } from './components/NotificationBell';
